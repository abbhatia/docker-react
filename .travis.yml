language: generic 
sudo: required
servies: 
  - docker

before_install:
  - docker build -t abhibhat97/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true abhibhat97/docker-react npm run test